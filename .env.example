# Solarys Fraud Detection Demo - Environment Configuration
# Copy this file to .env and fill in your values

# Database (PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/solarys_fraud

# Anthropic API (for Claude - Detection Agent)
ANTHROPIC_API_KEY=sk-ant-...

# Google API (for Gemini - Investigation Agent)
GOOGLE_API_KEY=...

# OpenAI API (for embeddings/semantic search)
OPENAI_API_KEY=sk-...

# API Server
PORT=3001

# Web App
NEXT_PUBLIC_API_URL=http://localhost:3001

# ===========================================
# Railway/Production Only (not needed for local dev)
# ===========================================

# Agent URLs (for inter-service A2A communication in Railway)
# Set these to the Railway service URLs when deploying
# DETECTION_AGENT_URL=https://your-detection-agent.up.railway.app
# INVESTIGATION_AGENT_URL=https://your-investigation-agent.up.railway.app

# Public URLs for agent cards (what each agent advertises as its own URL)
# Set these on EACH agent service to its own public URL
# DETECTION_AGENT_PUBLIC_URL=https://your-detection-agent.up.railway.app
# INVESTIGATION_AGENT_PUBLIC_URL=https://your-investigation-agent.up.railway.app

# MCP Server URL (if running separately)
# MCP_SERVER_URL=https://your-mcp-server.up.railway.app
